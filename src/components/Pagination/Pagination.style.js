import styled from 'styled-components';

const PaginationStyle = styled.div`
    text-align: right;
    font-weight: bold;
    padding: 10px;

    .separator {
        color: #ff732e;
    }

    .link {
        color: #ff732e;
    }
`;

export default PaginationStyle;