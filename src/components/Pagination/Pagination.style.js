import styled from 'styled-components';

const PaginationStyle = styled.div`
    text-align: right;
    font-weight: bold;
    padding: 10px;

    .separator {
        color: #ca4b0d;
    }

    .link {
        color: #ca4b0d;
    }
`;

export default PaginationStyle;